{
  "name": "muga",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node ./util/parseEntrants.js && tsc && vite build",
    "parse": "node ./util/parseEntrants.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.1"
  },
  "dependencies": {
    "rimraf": "^5.0.7"
  }
}
